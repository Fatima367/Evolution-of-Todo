apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todoboard.fullname" . }}-audit-service
  labels:
    {{- include "todoboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: audit-service
spec:
  replicas: {{ .Values.auditService.replicaCount }}
  selector:
    matchLabels:
      {{- include "todoboard.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: audit-service
  template:
    metadata:
      labels:
        {{- include "todoboard.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: audit-service
      annotations:
        {{- if .Values.auditService.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: "{{ .Values.auditService.dapr.appId }}"
        dapr.io/app-port: "{{ .Values.auditService.dapr.appPort }}"
        {{- end }}
    spec:
      containers:
      - name: audit-service
        image: "{{ .Values.auditService.image.repository }}:{{ .Values.auditService.image.tag }}"
        imagePullPolicy: {{ .Values.auditService.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8003
          protocol: TCP
        env:
        {{- range $key, $value := .Values.auditService.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          {{- toYaml .Values.auditService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoboard.fullname" . }}-audit-service
  labels:
    {{- include "todoboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: audit-service
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "todoboard.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: audit-service

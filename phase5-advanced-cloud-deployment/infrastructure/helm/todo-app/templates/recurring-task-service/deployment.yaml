{{- if .Values.recurringTaskService.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todoboard.fullname" . }}-recurring-task-service
  labels:
    {{- include "todoboard.recurringTaskService.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.recurringTaskService.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.recurringTaskService.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.recurringTaskService.rollingUpdate.maxUnavailable }}
  selector:
    matchLabels:
      {{- include "todoboard.recurringTaskService.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "todoboard.recurringTaskService.selectorLabels" . | nindent 8 }}
      annotations:
        {{- if .Values.recurringTaskService.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: "recurring-task-service"
        dapr.io/app-port: "{{ .Values.recurringTaskService.containerPort }}"
        dapr.io/log-level: "info"
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "todoboard.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.recurringTaskService.podSecurityContext | nindent 8 }}
      containers:
      - name: recurring-task-service
        image: {{ include "todoboard.recurringTaskService.image" . }}
        imagePullPolicy: {{ .Values.recurringTaskService.image.pullPolicy | default .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.recurringTaskService.containerPort }}
          protocol: TCP
        env:
        {{- range $key, $value := .Values.recurringTaskService.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: {{ include "todoboard.fullname" . }}-recurring-config
        - secretRef:
            name: {{ include "todoboard.fullname" . }}-secrets
        {{- if .Values.recurringTaskService.livenessProbe.enabled }}
        livenessProbe:
          httpGet:
            path: {{ .Values.recurringTaskService.livenessProbe.httpGet.path }}
            port: {{ .Values.recurringTaskService.livenessProbe.httpGet.port }}
            scheme: HTTP
          initialDelaySeconds: {{ .Values.recurringTaskService.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.recurringTaskService.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.recurringTaskService.livenessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.recurringTaskService.livenessProbe.successThreshold }}
          failureThreshold: {{ .Values.recurringTaskService.livenessProbe.failureThreshold }}
        {{- end }}
        {{- if .Values.recurringTaskService.readinessProbe.enabled }}
        readinessProbe:
          httpGet:
            path: {{ .Values.recurringTaskService.readinessProbe.httpGet.path }}
            port: {{ .Values.recurringTaskService.readinessProbe.httpGet.port }}
            scheme: HTTP
          initialDelaySeconds: {{ .Values.recurringTaskService.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.recurringTaskService.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.recurringTaskService.readinessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.recurringTaskService.readinessProbe.successThreshold }}
          failureThreshold: {{ .Values.recurringTaskService.readinessProbe.failureThreshold }}
        {{- end }}
        resources:
          {{- toYaml .Values.recurringTaskService.resources | nindent 10 }}
        securityContext:
          {{- toYaml .Values.recurringTaskService.securityContext | nindent 10 }}
      {{- with .Values.recurringTaskService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.recurringTaskService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.recurringTaskService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
